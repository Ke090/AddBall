import { describe,expect,it } from 'vitest';
import { ActionQueue,mergeAreas,splitArea,totalArea } from '../src/game/rules';
describe('area rules',()=>{it.each([[1,1,2],[3,7,10]])('merges %i + %i = %i',(a,b,result)=>expect(mergeAreas(a,b)).toBe(result));it.each([[2,[1,1]],[3,[1,2]],[10,[5,5]],[11,[5,6]]] as const)('splits %i conservatively',(area,result)=>expect(splitArea(area)).toEqual(result));it('does not split area 1',()=>expect(splitArea(1)).toBeNull());it('conserves total area through a sequence',()=>{let areas=Array(30).fill(1) as number[];areas.splice(0,2,mergeAreas(areas[0],areas[1]));const parts=splitArea(areas[0]);expect(parts).not.toBeNull();areas.splice(0,1,...parts!);expect(totalArea(areas)).toBe(30);});});
describe('collision action queue',()=>{it('reserves balls against duplicate same-step actions',()=>{const queue=new ActionQueue();expect(queue.queue({type:'merge',a:1,b:2})).toBe(true);expect(queue.queue({type:'merge',a:1,b:3})).toBe(false);expect(queue.queue({type:'split',id:2,normal:{x:0,y:1}})).toBe(false);expect(queue.drain()).toHaveLength(1);expect(queue.queue({type:'split',id:1,normal:{x:1,y:0}})).toBe(true);});});
