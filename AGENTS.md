# AddBall 開発ルール

## プロジェクト

- このリポジトリは、一般公開を前提としたスマートフォン優先のブラウザ向け2D物理ゲームです。試作品ではなく、完成品として扱ってください。
- ゲームの挙動については `docs/GAME_SPEC.md` を唯一の正とします。
- 主な対象環境は iPhone Safari、Android Chrome、およびデスクトップ版の Chrome、Safari、Firefox、Edge です。

## 技術方針

- TypeScript、Vite、HTML/CSS、Canvas、Matter.js、PWAを使用します。クライアントサイドのみで動作し、静的ホスティングへ配置できる構成を維持してください。
- 不要なバックエンド、ユーザーアカウント、広告、アクセス解析、トラッキング、傾きセンサーデータの送信、不要な外部通信、実行時CDN依存、明確な利点のない大規模フレームワーク、`eval`、埋め込みシークレットを追加してはいけません。
- ブラウザ標準APIと、責務を絞った小さなモジュールを優先してください。この規模で明確な利点が生じない限り、Reactは導入しません。

## アーキテクチャ

- 可能な限り、物理演算、ゲームルール、衝突処理、合体・分裂処理、描画、ポインター入力、端末傾き、音声、UI、設定の責務を分離してください。巨大な単一ファイルへ実装を集中させてはいけません。
- ゲームバランス値は設定モジュールへ集約し、同じゲームルールを複数箇所へ実装しないでください。
- **最重要不変条件:** ゲーム上の面積は整数で、常に `玉の総面積 = 30` です。生成、合体、分裂、再スタートのすべてで維持し、開発・デバッグ時には検証してください。
- 衝突コールバック内で無秩序にワールドを変更してはいけません。「衝突検出 → アクションキュー → 物理ステップ終了 → 合体・分裂の適用」の順序を守ってください。各ステップで玉を一度だけ予約し、二重合体、合体後の分裂、複数相手との合体を防いでください。

## モバイルと描画

- 端末傾きAPIが未対応、利用不可、拒否済みの場合や、デスクトップ環境でもゲームを遊べる状態を維持してください。傾きセンサーの許可は、明示的なユーザー操作の後にだけ要求してください。
- タッチとマウスの双方に対応するため、Pointer Eventsを使用してください。
- 独自のCanvas rendererを使用し、Matter.jsのデバッグ描画を製品UIに使用してはいけません。devicePixelRatioへ対応し、論理上の10×10フィールドを画面ピクセルから分離してください。
- 現行スマートフォンで60fpsを目標とします。`requestAnimationFrame` を使い、大きなdeltaを制限し、バックグラウンド復帰後の暴走を防ぎ、ゲームループ内の不要なDOM更新を避けてください。

## テストとスコープ

- 変更完了前に `npm run test` と `npm run build` を実行してください。利用可能な場合は型チェックとlintも実行します。
- ゲームルールのテストでは、合体後の面積、分裂後の面積、面積1が分裂できないこと、総面積の保存、衝突アクションの重複防止を検証してください。
- 無関係なコードを書き換えない、`GAME_SPEC.md` を無断で変更しない、TODOを完成扱いしない、一時的なデバッグコードを製品へ残さない、という原則を守ってください。

